# お問い合わせフォーム 仕様書

## 1. 概要

Laravelで実装されたお問い合わせフォームアプリケーション。エンドユーザー向けの「お問い合わせフォーム」と、管理者向けの「管理ページ」の2つの画面群からなる。

## 2. 技術仕様

| 項目 | 内容 |
|---|---|
| フレームワーク | Laravel 13（PHP ^8.3） |
| データベース | SQLite（`database/database.sqlite`） |
| ORM | Eloquent |
| フロントエンド | Blade テンプレート + 素のCSS（Node/npm未導入環境のためVite/Tailwindは未使用） |
| テスト | PHPUnit（Feature テスト） |
| コーディング規約 | PSR-12準拠、コメントは日本語 |
| 実行環境 | ローカル: `php artisan serve` / Docker: `docker compose up`（`Dockerfile`, `docker-compose.yml`） |

## 3. データベース設計

### `contacts` テーブル

| カラム名 | 型 | 必須 | 説明 |
|---|---|---|---|
| id | bigint (PK) | - | 主キー |
| name | string | ○ | お名前 |
| email | string | ○ | メールアドレス |
| subject | string | ○ | 件名（自由記述） |
| message | text | ○ | お問い合わせ本文 |
| status | string | ○ | 対応状況。`new` / `in_progress` / `resolved`。デフォルト `new` |
| created_at / updated_at | timestamp | - | Laravel標準タイムスタンプ |

マイグレーションファイル: `database/migrations/2026_08_18_133907_create_contacts_table.php`

### ステータス enum（`app/Enums/ContactStatus.php`）

PHP 8.1 backed enum として定義。

| 値 | 表示ラベル |
|---|---|
| `new` | 新規 |
| `in_progress` | 対応中 |
| `resolved` | 解決済み |

`label()` メソッドで管理画面表示用の日本語ラベルを返す。

### モデル（`app/Models/Contact.php`）

- `#[Fillable(['name', 'email', 'subject', 'message', 'status'])]`（Laravel 13の属性ベースFillable指定）
- `status` カラムを `ContactStatus` enum にキャスト
- `HasFactory` トレイトを使用（`database/factories/ContactFactory.php`でテスト用ダミーデータ生成）

## 4. 機能仕様

### 4.1 公開側：お問い合わせフォーム

入力 → 確認 → 送信 → 完了 の4ステップ。確認画面は「隠しフィールド再送信方式」（入力値をhiddenフィールドに埋め込んで確認画面から再POSTする）で実装。

| # | 画面 | メソッド/パス | ルート名 | コントローラーアクション | ビュー |
|---|---|---|---|---|---|
| 1 | 入力フォーム | GET `/contact` | `contact.create` | `ContactController@create` | `contacts/create.blade.php` |
| 2 | 確認画面 | POST `/contact/confirm` | `contact.confirm` | `ContactController@confirm` | `contacts/confirm.blade.php` |
| 3 | 送信処理 | POST `/contact` | `contact.store` | `ContactController@store` | （リダイレクトのみ） |
| 4 | 完了画面 | GET `/contact/complete` | `contact.complete` | `ContactController@complete` | `contacts/complete.blade.php` |

**処理の流れ:**
1. `create`: 入力フォームを表示
2. `confirm`: `StoreContactRequest` でバリデーション後、確認画面に入力内容を表示（値はhiddenフィールドに保持）
3. `store`: 確認画面からの送信を再度 `StoreContactRequest` でバリデーションし、`Contact::create()` でDB保存。セッションに `contact_submitted` フラグを立てて完了画面へリダイレクト（PRGパターン）
4. `complete`: セッションの `contact_submitted` フラグを確認（`session()->pull()`で消費）。フラグが無い場合（直接アクセス等）は入力画面へリダイレクトし、あれば「お問い合わせありがとうございました」の完了メッセージを表示

### 4.2 管理側：管理ページ

一覧 → 詳細 → ステータス変更 の流れ。**ログイン必須**（`auth`ミドルウェアで保護）。

| # | 画面 | メソッド/パス | ルート名 | コントローラーアクション | ビュー |
|---|---|---|---|---|---|
| 1 | 一覧 | GET `/admin/contacts` | `admin.contacts.index` | `Admin\ContactController@index` | `admin/contacts/index.blade.php` |
| 2 | 詳細・ステータス変更 | GET `/admin/contacts/{contact}` | `admin.contacts.show` | `Admin\ContactController@show` | `admin/contacts/show.blade.php` |
| - | ステータス更新処理 | PATCH `/admin/contacts/{contact}` | `admin.contacts.update` | `Admin\ContactController@update` | （リダイレクトのみ） |

**処理の流れ:**
1. `index`: `Contact::latest()->paginate(20)` で新着順に一覧をページネーション表示
2. `show`: ルートモデルバインディングで対象の`Contact`を取得し、詳細内容とステータス選択肢（`ContactStatus::cases()`）を表示
3. `update`: `UpdateContactStatusRequest`（`Rule::enum(ContactStatus::class)`で値を検証）でステータスを更新し、詳細画面へリダイレクト（更新完了メッセージをフラッシュ表示）

### 4.3 管理者ログイン

Laravelの標準セッション認証（`Auth::attempt`、`users`テーブル）を使用。ロールや権限の区分は無く、ログインできれば管理ページ全体を操作できる。

| # | 画面/処理 | メソッド/パス | ルート名 | コントローラーアクション | ビュー |
|---|---|---|---|---|---|
| 1 | ログインフォーム | GET `/login` | `login` | `Auth\LoginController@create` | `auth/login.blade.php` |
| - | ログイン処理 | POST `/login` | `login.store` | `Auth\LoginController@store` | （リダイレクトのみ） |
| - | ログアウト処理 | POST `/logout` | `logout` | `Auth\LoginController@destroy` | （リダイレクトのみ） |

**処理の流れ:**
1. `create`: ログインフォームを表示（`guest`ミドルウェアによりログイン済みならアクセス不可）
2. `store`: `LoginRequest::authenticate()`で`Auth::attempt()`を実行し、失敗時はバリデーションエラーとしてメールアドレス欄にメッセージ表示。成功時はセッションを再生成し、`intended()`で元々アクセスしようとしていたURL（無ければ管理一覧）へリダイレクト
3. `destroy`: ログアウトし、セッションを無効化・トークン再生成した上でログイン画面へリダイレクト
4. `/admin/*` 配下のルートは全て`auth`ミドルウェアで保護されており、未ログインでアクセスすると`/login`へリダイレクトされる

**初期アカウント:** `database/seeders/DatabaseSeeder.php`で作成（`User::updateOrCreate`のため再実行しても重複しない）。

| メールアドレス | パスワード |
|---|---|
| admin@example.com | password |

## 5. バリデーションルール

### `StoreContactRequest`（入力フォーム・確認画面共通）

| 項目 | ルール |
|---|---|
| name（お名前） | 必須、文字列、255文字以内 |
| email（メールアドレス） | 必須、メール形式、255文字以内 |
| subject（件名） | 必須、文字列、255文字以内 |
| message（お問い合わせ内容） | 必須、文字列、2000文字以内 |

日本語の属性名（`attributes()`）を定義しており、エラーメッセージに項目名が日本語で表示される。

### `UpdateContactStatusRequest`

| 項目 | ルール |
|---|---|
| status | 必須、`ContactStatus` enumの値（`new`/`in_progress`/`resolved`）のいずれか |

## 6. ディレクトリ構成（主要ファイル）

```
app/
├── Enums/ContactStatus.php
├── Http/
│   ├── Controllers/
│   │   ├── ContactController.php          # 公開側
│   │   ├── Admin/ContactController.php    # 管理側
│   │   └── Auth/LoginController.php       # 認証
│   └── Requests/
│       ├── StoreContactRequest.php
│       ├── UpdateContactStatusRequest.php
│       └── LoginRequest.php
└── Models/Contact.php・User.php

database/
├── migrations/2026_08_18_133907_create_contacts_table.php
├── factories/ContactFactory.php
└── seeders/DatabaseSeeder.php      # 管理者初期アカウント作成

resources/views/
├── layouts/app.blade.php          # 共通レイアウト
├── contacts/
│   ├── create.blade.php
│   ├── confirm.blade.php
│   └── complete.blade.php
├── admin/contacts/
│   ├── index.blade.php
│   └── show.blade.php
└── auth/login.blade.php

routes/web.php

tests/Feature/
├── ContactFormTest.php
├── Admin/ContactStatusTest.php
└── Auth/LoginTest.php

Dockerfile / docker-compose.yml / docker/entrypoint.sh
```

## 7. テスト

PHPUnit Featureテスト、計16件（既存の `ExampleTest` を除く）。

**`tests/Feature/ContactFormTest.php`**
- 入力フォームが表示される
- 妥当な入力で確認画面に入力内容が表示される
- 不正な入力（必須項目欠落・email形式不正）は確認画面に進めない
- 確認画面から送信すると保存され完了画面へリダイレクトされる
- 完了画面が表示される
- 未送信で完了画面に直接アクセスすると入力画面へリダイレクトされる

**`tests/Feature/Admin/ContactStatusTest.php`**（各テストで`actingAs()`によりログイン状態を作成）
- 一覧に登録済みのお問い合わせが表示される
- 詳細画面が表示される
- ステータスを変更できる
- 不正なステータス値は拒否される
- 未ログインでは一覧にアクセスできずログイン画面へリダイレクトされる

**`tests/Feature/Auth/LoginTest.php`**
- ログインフォームが表示される
- 正しい認証情報でログインでき管理一覧へリダイレクトされる
- 誤った認証情報ではログインできない
- ログアウトできる

## 8. 実行方法

**ローカル（PHP環境がある場合）**
```
php artisan migrate
php artisan db:seed
php artisan serve
```

**Docker（推奨・自己完結）**
```
docker compose up
```
起動後、`http://localhost:8000/contact`（公開フォーム）、`http://localhost:8000/admin/contacts`（管理ページ、要ログイン）でアクセス可能。管理ページのログイン情報は 4.3 節の初期アカウントを参照。

## 9. スコープ外（未実装）

- ロール・権限の区分（ログインできれば誰でも管理ページ全体を操作可能）
- 新規登録・パスワード再設定などのアカウント管理機能
- ファイル添付機能
- メール通知（お問い合わせ受付時の自動返信等）
- お問い合わせ種別・カテゴリの選択式化（件名は自由記述のみ）
